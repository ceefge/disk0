<Window x:Class="DriveScan.Views.AnalysisWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="disk0 - Analysis" Height="600" Width="950"
        Background="#FF1E1E2E" WindowStyle="None" ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <Border Grid.Row="0" Background="#FF18182A" MouseLeftButtonDown="TitleBar_MouseDown">
            <Grid Height="30">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="TitleText" Foreground="#FFAAAACC" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0"/>
                <Button Grid.Column="1" Content="&#x2715;" Click="Close_Click"
                        Background="Transparent" Foreground="#FFAAAACC" BorderThickness="0" Width="36" Height="30" FontSize="14" Cursor="Hand">
                    <Button.Template><ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                        <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FFE81123"/><Setter Property="Foreground" Value="White"/></Trigger>
                        </ControlTemplate.Triggers></ControlTemplate></Button.Template>
                </Button>
            </Grid>
        </Border>

        <!-- Status -->
        <Border Grid.Row="1" Background="#FF22223A" Padding="12,6">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="StatusText" Foreground="#FFAAAACC" FontSize="12"/>
                <TextBlock Grid.Column="1" x:Name="SummaryText" Foreground="#FF66CC88" FontSize="12" Margin="20,0,0,0"/>
                <TextBlock Grid.Column="3" x:Name="ProgressText" Foreground="#FFFFCC33" FontSize="12" FontWeight="Bold"/>
            </Grid>
        </Border>

        <!-- Results with checkboxes -->
        <DataGrid Grid.Row="2" x:Name="ResultsGrid"
                  Background="#FF1E1E2E" Foreground="White" BorderThickness="0"
                  AutoGenerateColumns="False" IsReadOnly="False" SelectionMode="Extended"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#FF333355"
                  RowBackground="#FF1E1E2E" AlternatingRowBackground="#FF252540"
                  HeadersVisibility="Column" FontSize="12" CanUserSortColumns="True"
                  MouseDoubleClick="ResultsGrid_DoubleClick">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#FF2A2A3E"/><Setter Property="Foreground" Value="#FFAAAACC"/>
                    <Setter Property="Padding" Value="8,4"/><Setter Property="FontSize" Value="12"/>
                    <Setter Property="BorderBrush" Value="#FF333355"/><Setter Property="BorderThickness" Value="0,0,1,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Binding="{Binding IsChecked, UpdateSourceTrigger=PropertyChanged}" Width="30" Header="">
                    <DataGridCheckBoxColumn.ElementStyle>
                        <Style TargetType="CheckBox"><Setter Property="HorizontalAlignment" Value="Center"/></Style>
                    </DataGridCheckBoxColumn.ElementStyle>
                    <DataGridCheckBoxColumn.EditingElementStyle>
                        <Style TargetType="CheckBox"><Setter Property="HorizontalAlignment" Value="Center"/></Style>
                    </DataGridCheckBoxColumn.EditingElementStyle>
                </DataGridCheckBoxColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle><Style TargetType="TextBlock">
                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/><Setter Property="Foreground" Value="#FFDDDDEE"/>
                    </Style></DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn x:Name="ColSize" Header="Size" Binding="{Binding SizeText}" Width="100" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle><Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#FFAADDFF"/><Setter Property="TextAlignment" Value="Right"/>
                    </Style></DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn x:Name="ColDetails" Header="Details" Binding="{Binding Detail}" Width="250" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle><Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#FF8899BB"/><Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                    </Style></DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn x:Name="ColCategory" Header="Category" Binding="{Binding Category}" Width="110" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle><Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#FFCCAA55"/>
                    </Style></DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Bottom bar with action buttons -->
        <Border Grid.Row="3" Background="#FF18182A" Padding="8,6">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <Button Grid.Column="0" x:Name="BtnSelectAll" Content="Select All" Padding="10,4" Click="SelectAll_Click"
                        Background="#FF3A3A5A" Foreground="White" BorderBrush="#FF5A5A8A" Cursor="Hand" Margin="0,0,6,0"/>
                <Button Grid.Column="1" x:Name="BtnDeselectAll" Content="Deselect All" Padding="10,4" Click="DeselectAll_Click"
                        Background="#FF3A3A5A" Foreground="White" BorderBrush="#FF5A5A8A" Cursor="Hand" Margin="0,0,6,0"/>
                <Button Grid.Column="2" x:Name="BtnDeleteSelected" Content="Delete Selected" Padding="10,4" Click="DeleteSelected_Click"
                        Background="#FF6A2A2A" Foreground="#FFFF8888" BorderBrush="#FF8A4A4A" Cursor="Hand" FontWeight="SemiBold"/>
                <Button Grid.Column="4" x:Name="BtnOpenExplorer" Content="Open in Explorer" Padding="10,4" Click="OpenInExplorer_Click"
                        Background="#FF3A3A5A" Foreground="White" BorderBrush="#FF5A5A8A" Cursor="Hand" Margin="0,0,6,0"/>
                <Button Grid.Column="5" x:Name="BtnClose" Content="Close" Padding="10,4" Click="Close_Click"
                        Background="#FF3A3A5A" Foreground="White" BorderBrush="#FF5A5A8A" Cursor="Hand"/>
            </Grid>
        </Border>
    </Grid>
</Window>
